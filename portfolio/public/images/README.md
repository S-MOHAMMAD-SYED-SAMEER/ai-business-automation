# Project 1 screenshots

Real screenshots of the running Sales-Recovery demo, referenced by the case
study at `src/pages/SalesRecoveryCaseStudy.tsx`. Until a file exists, its slot
on the page shows a neutral "Product screenshot to be added" placeholder —
adding the file is all that is needed, no code change.

Capture against the deployed demo with both services warm and the knowledge
base populated, browser window ~1200-1400px wide, no browser chrome:

| File | What to capture |
|---|---|
| `p1-chat-overview.png` | The full demo page a few messages in — the hero visual |
| `p1-knowledge-answer.png` | The international-shipping answer with its "Checked store information" badge |
| `p1-tool-lookup.png` | An order or stock reply with its tool badge |
| `p1-sales-signal.png` | The "still deciding / a bit pricey" reply showing both "Noticed:" badges |

Deliberately not filled with mock-ups: an illustration presented as a
screenshot would misrepresent what the product looks like.
