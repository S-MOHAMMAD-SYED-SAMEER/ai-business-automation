# No environment variables are required for the portfolio site (static React app).
# This file exists to establish the pattern: as later projects (chatbot, outreach
# engine, ATS) introduce API keys and secrets, add their variable names here
# (values blank/placeholder only) and keep the real .env gitignored.
